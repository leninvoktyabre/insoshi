<h1>Editing comment</h1>

<%= error_messages_for :comment %>

<% form_for([@advert, @comment]) do |f| %>
  <p>
    <b>Body</b><br />
    <%= f.text_area :body %>
  </p>

	<p>
    <%= f.submit "Edit" %>
  </p>

<% end %>

<%= link_to 'Back', advert_path(@advert) %>
