<h2><%= @person.name %>'s Pets </h2>

<% if pets.size > 0 %>

  <table>
    <tr>
      <th>Name</th>
      <th>Species</th>
      <th>Breed</th>
      <th>Sex</th>
      <th>Date of birth</th>
      <th>Color</th>
      <th>Favorite food</th>
      <th>Description</th>
      <th colspan="2">Controls</th>
    </tr>
      <%= render :partial => "shared/pet", :collection => pets %>
  </table>

<% end %>

<% if current_person?(@person)%>
  <%= link_to "Add pet", new_person_pet_path(@person) %>
<% end %>
